#include <stdio.h>

int main(int argc, char *argv[] )
{   
    #
    int distance = 100;

    int i;

    for (i = 0; i <= 5; i++)
    {
        printf(" I'll count to 5, %d\n",i);
    }
    printf("You are %d miles away\n", distance);
    
    return (0);
}